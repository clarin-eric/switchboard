package eu.clarin.switchboard.profiler.api;

/**
 * Enumeration specifying the confidence we have that a profile is correctly detected or not.
 */
public enum Confidence {
    Uncertain,
    Certain,
}
